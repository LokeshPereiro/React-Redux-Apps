import { SuperBalls } from "@uiball/loaders";

export const Loader = () => {
  return (
    <div className="container-loader">
      <SuperBalls size={55} speed={1.4} color="red" />
    </div>
  );
};
